//! SAML connection CRUD route handlers — implemented in Task 2.
//!
//! Placeholder so the `sso` module tree compiles after Task 1 (core modules).
//! Task 2 fills in `create_connection`, `list_connections`, `delete_connection`:
//! SSRF-guarded `metadataUrl` (SSO-04), CA pre-flight (SSO-02), Polis POST, the
//! Kratos `provider: generic` write via the AUTH-04 `providers[]` path (SSO-03),
//! and the two-sided delete.
