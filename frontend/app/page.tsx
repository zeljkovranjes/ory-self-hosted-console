// Placeholder landing page. This exists ONLY to prove the frontend container
// renders in the compose graph (Plan 04). The real admin shell — navigation,
// data tables, settings forms, Monaco editors — is Phase 5. Do not build UI here.

export default function Home() {
  return (
    <main>
      <h1>Ory Self-Hosted Console</h1>
      <p>Skeleton frontend is running. The console UI lands in Phase 5.</p>
    </main>
  );
}
